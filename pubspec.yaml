name: private_domain_drive_client
description: Private Domain Drive Flutter client.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  crypto: ^3.0.6
  convert: ^3.1.2
  flutter_secure_storage: ^11.2.0
  http: ^1.2.2
  file_picker: ^13.1.0
  path_provider: ^2.1.5
  package_info_plus: ^10.2.1
  url_launcher: ^6.3.2
  private_domain_oss:
    path: packages/private_domain_oss
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  file_picker_platform_interface: ^4.0.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
